
version: 2

models:
  - name: aliases
    tests:
      - fully_queryable:
          nullable_columns: [primaryNames]

    columns:
      - name: nconst
        tests:
          - unique
      - name: primaryNames
        