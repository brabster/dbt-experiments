
version: 2

models:
  - name: aliases
    tests:
      - none_null:
          except_columns: [primaryNames]
          force_table_scan: True

    columns:
      - name: nconst
        tests:
          - unique
      - name: primaryNames
        